/* const { Branch } = require("../../db");

const getBranchByIdController = async (id) => {
	try {
		const branch = await Branch.findByPk(id);
		return branch;
	} catch (error) {
		throw new Error("Error retrieving branch from database.");
	}
};

module.exports = { getBranchByIdController };
 */